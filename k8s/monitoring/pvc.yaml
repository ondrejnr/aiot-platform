apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: influxdb-pvc-piraeus
  namespace: monitoring
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: piraeus-storage
  resources:
    requests:
      storage: 10Gi
