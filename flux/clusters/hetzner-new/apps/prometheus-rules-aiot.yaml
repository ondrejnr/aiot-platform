---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prometheus-rules-aiot
  namespace: monitoring
spec:
  suspend: false
  interval: 30m
  releaseName: prometheus-rules-aiot
  chart:
    spec:
      chart: ./apps/prometheus-rules-aiot
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  install:
    createNamespace: false
  upgrade:
    cleanupOnFail: true
