image:
  repository: python
  tag: 3.12-slim
  pullPolicy: IfNotPresent

prometheusUrl: http://prometheus-kube-prometheus-prometheus.monitoring.svc:9090
pollIntervalSeconds: 60
fallbackNamespace: monitoring

resources:
  requests:
    cpu: 10m
    memory: 48Mi
  limits:
    memory: 128Mi
