# Bootstrap secret used by CNPG to create the awx role with this password
apiVersion: v1
kind: Secret
metadata:
  name: awx-pg-credentials
  namespace: databases
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.postgres.username }}
  password: {{ .Values.postgres.password | quote }}
