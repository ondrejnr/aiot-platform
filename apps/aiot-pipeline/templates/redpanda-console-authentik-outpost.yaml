{{- /*
Removed: authentik-outpost-proxy Service + aiot-redpanda-console-authentik-outpost Ingress.
These pointed at a non-existent authentik outpost (authentik-server returned 404 on
/outpost.goauthentik.io/*), causing nginx to return 500 for redpanda.46.4.123.8.nip.io.
Redpanda Console now authenticates via the shared oauth2-proxy (see ingress.yaml).
*/ -}}
