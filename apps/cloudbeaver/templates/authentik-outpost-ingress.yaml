# CloudBeaver uses the shared oauth2-proxy (see service-ingress.yaml auth-url),
# so the authentik outpost ingress is no longer needed and was removed to avoid
# routing to a non-existent authentik-outpost-proxy service (which caused nginx 500).
