apiVersion: batch/v1
kind: CronJob
metadata:
  name: aiot-maintenance-train
  namespace: {{ .Values.training.namespace }}
  labels:
    {{- include "aiotml.labels" . | nindent 4 }}
    app.kubernetes.io/component: trainer
spec:
  schedule: {{ .Values.training.schedule | quote }}
  suspend: {{ .Values.training.suspend }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 3600
      template:
        metadata:
          labels:
            app.kubernetes.io/component: trainer
            app.kubernetes.io/part-of: aiot-ml-platform
        spec:
          restartPolicy: Never
          containers:
            - name: trainer
              image: {{ .Values.training.image | quote }}
              imagePullPolicy: IfNotPresent
              command: ["/bin/sh", "-c"]
              args:
                - |
                  set -e
                  python -m pip install --no-cache-dir pandas scikit-learn psycopg2-binary "mlflow=={{ .Values.mlflow.version }}" >/tmp/train-pip.log 2>&1
                  exec python /app/train.py
              envFrom:
                - secretRef:
                    name: aiot-pg-credentials
              env:
                - name: MLFLOW_TRACKING_URI
                  value: http://mlflow.mlflow.svc.cluster.local:5000
                - name: MODEL_NAME
                  value: {{ .Values.inference.modelName | quote }}
                - name: EXPERIMENT_NAME
                  value: aiot-maintenance-proxied
                - name: LOOKBACK_DAYS
                  value: {{ .Values.training.lookbackDays | quote }}
                - name: MAX_ROWS
                  value: {{ .Values.training.maxRows | quote }}
              resources:
                {{- toYaml .Values.training.resources | nindent 16 }}
              volumeMounts:
                - name: code
                  mountPath: /app
          volumes:
            - name: code
              configMap:
                name: aiot-maintenance-trainer-code
