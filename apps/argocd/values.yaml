# Live helm values from cluster (snapshot 2026-04-27T05:54:07Z)
# Upstream chart key: argo-cd
argo-cd:
  applicationSet:
    enabled: false
  configs:
    cm:
      admin.enabled: "true"
      exec.enabled: "false"
      statusbadge.enabled: "false"
      timeout.reconciliation: 180s
      url: https://argocd.35.241.255.137.nip.io
    params:
      server.insecure: true
    rbac:
      policy.csv: |
        p, role:org-admin, applications, *, */*, allow
        p, role:org-admin, clusters, get, *, allow
        p, role:org-admin, repositories, *, *, allow
        p, role:org-admin, projects, *, *, allow
        g, admin, role:org-admin
      policy.default: role:readonly
  controller:
    metrics:
      enabled: true
  dex:
    enabled: false
  global:
    domain: argocd.35.241.255.137.nip.io
  notifications:
    enabled: false
  repoServer:
    metrics:
      enabled: true
  server:
    ingress:
      annotations:
        nginx.ingress.kubernetes.io/backend-protocol: HTTP
        nginx.ingress.kubernetes.io/ssl-redirect: "true"
      enabled: true
      hostname: argocd.35.241.255.137.nip.io
      ingressClassName: nginx
      tls: false
    metrics:
      enabled: true
