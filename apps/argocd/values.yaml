# Live helm values from cluster (snapshot 2026-04-27T05:54:07Z)
# Upstream chart key: argo-cd
argo-cd:
  applicationSet:
    enabled: false
  configs:
    cm:
      admin.enabled: "true"
      exec.enabled: "false"
      statusbadge.enabled: "false"
      timeout.reconciliation: 180s
      url: https://argocd.46.4.123.8.nip.io
      oidc.config: |
        name: Authentik
        issuer: https://authentik.46.4.123.8.nip.io/application/o/argocd/
        clientID: argocd
        clientSecret: $oidc.authentik.clientSecret
        requestedScopes:
          - openid
          - profile
          - email
          - groups
    params:
      server.insecure: true
    rbac:
      policy.csv: |
        p, role:org-admin, applications, *, */*, allow
        p, role:org-admin, clusters, get, *, allow
        p, role:org-admin, repositories, *, *, allow
        p, role:org-admin, projects, *, *, allow
        g, admin, role:org-admin
      policy.default: role:readonly
  controller:
    replicas: 1
    metrics:
      enabled: true
  dex:
    enabled: false
  global:
    domain: argocd.46.4.123.8.nip.io
  notifications:
    enabled: false
  repoServer:
    metrics:
      enabled: true
  server:
    ingress:
      enabled: true
      ingressClassName: nginx
      hostname: argocd.46.4.123.8.nip.io
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        nginx.ingress.kubernetes.io/backend-protocol: HTTP
        nginx.ingress.kubernetes.io/ssl-redirect: "true"
      hosts:
        - argocd.46.4.123.8.nip.io
      tls:
        - hosts:
            - argocd.46.4.123.8.nip.io
          secretName: argocd-tls
    metrics:
      enabled: true
