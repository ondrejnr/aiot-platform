# Karpor v0.6.4 with patched UI to re-enable cluster Events tab
# Image built locally and imported to k3d (localhost:5000/karpor:v0.6.4-patched)
karpor:
  server:
    image:
      repo: localhost:5000/karpor
      tag: v0.6.4-patched
  syncer:
    image:
      repo: localhost:5000/karpor
      tag: v0.6.4-patched
  job:
    image:
      repo: localhost:5000/karpor
      tag: v0.6.4-patched
