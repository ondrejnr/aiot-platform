peConsole:
  # External PE master IP:port the Service Endpoints should point at.
  # 172.18.0.1 = k3d docker bridge gateway = the host (PE master native install)
  externalIP: "172.18.0.1"
  externalPort: 4443
  ingress:
    enabled: true
    host: pe.46.4.123.8.nip.io
    className: nginx
    clusterIssuer: letsencrypt-prod
  authentik:
    enabled: true
    host: authentik.46.4.123.8.nip.io
    serverService: authentik-server.authentik.svc.cluster.local
  autoLogin:
    enabled: true
    username: "admin"
    password: "AioT2026pe-Admin"
