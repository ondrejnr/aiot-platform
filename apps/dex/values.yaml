dex:
  ingress:
    enabled: true
    className: nginx
    hosts:
      - host: dex.142.132.207.168.nip.io
        paths: [{ path: /, pathType: Prefix }]
  config:
    issuer: https://dex.142.132.207.168.nip.io
    storage:
      type: kubernetes
      config: { inCluster: true }
    enablePasswordDB: true
    staticPasswords: []   # populate via secrets/auth/dex-passwords.yaml (sops)
